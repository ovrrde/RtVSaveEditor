# Normalize all text files to LF in the repo and on checkout, on every OS.
# (Without eol=lf, Windows checkouts get CRLF and break byte-exact tests.)
* text=auto eol=lf

# Test fixtures and Godot resources are always LF.
*.tres text eol=lf
*.rs   text eol=lf
